<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#3C3B3B"
    tools:context="techno.com.tynpu.Fragment.PromotionFragment">

    <ProgressBar
        android:id="@+id/progressBarpromotion"
        style="?android:attr/progressBarStyleLarge"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:indeterminate="true"
        android:indeterminateTint="@color/white"
        android:indeterminateTintMode="src_atop"
        android:visibility="gone">

    </ProgressBar>


    <android.support.v7.widget.RecyclerView
        android:id="@+id/task_promotion_rec"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginBottom="51dp"
        />

    <Button
        android:id="@+id/btn_profilter"
        android:layout_width="90dp"
        android:layout_height="35dp"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="90dp"
        android:background="@drawable/filter_btn"
        android:text="Filter"
        android:textAllCaps="false"
        android:textColor="#fff"
        android:textSize="7pt"

        />

</RelativeLayout>
